#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * _strlen - function that returns the length of a string
 * @s: Our pointer argument
 * Return: Always (success)
 */
int _strlen(char *s)
{
	return (strlen(s));

}

