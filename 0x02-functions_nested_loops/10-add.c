#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * add - A function that adds two integers and returns the result
 * @x: Parameter 1
 * @y: Parameter 2
 * Return: Always (success)
 */
int add(int x, int y)
{

	return (x + y);

}
