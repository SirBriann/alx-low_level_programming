#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * _puts - unction that prints a string
 * @str: Pointer argument
 * Return: Always (success)
 */
void _puts(char *str)
{
	printf("%s\n", str);
}
