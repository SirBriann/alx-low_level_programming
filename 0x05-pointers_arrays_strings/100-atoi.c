#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _atoi - unction that convert a string to an integer
 * @s: Our argument parameter
 * Return: Always (success)
 */
int _atoi(char *s)
{
	_putchar(s);

	return (s);
}
